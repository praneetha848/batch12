version: 2

exposures:
  - name: weekly_metrics
    label: saisri by the Week
    type: dashboard
    maturity: high
    description: 
      For more exposure about project details

    depends_on:
       - ref('status')
       - ref('order_merge')
       - ref('cust')
       - ref('customer_data')
       - ref('emp_seed_data')
       - ref('employees_salary')
       - ref('emp')
       - source('datafeed_shared_schema','raw_orders')
       - source('datafeed_shared_schema','raw_customer')
       - source('datafeed_shared_schema','status')
    owner:
     name: saisri
     email: snowflaketraining01@gmail.com
